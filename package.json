{
  "name": "gitbook-plugin-gen-all",
  "version": "1.0.0",
  "description": "GitBook plugin to generate pages not in the summary",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "gitbook": ">3.0.0"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cheerio": "^0.22.0",
    "fs-extra": "^1.0.0",
    "glob": "^7.1.1",
    "pify": "^2.3.0",
    "test-anything-protocol": "^0.1.0"
  },
  "devDependencies": {
    "ava": "^0.17.0",
    "unique-temp-dir": "^1.0.0"
  }
}
